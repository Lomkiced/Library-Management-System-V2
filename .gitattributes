# Auto detect text files and perform LF normalization
* text=auto

# Enforce Unix line endings for shell scripts so they don't crash in Docker
*.sh text eol=lf
docker/entrypoint.sh text eol=lf
docker/nginx/generate-ssl.sh text eol=lf
